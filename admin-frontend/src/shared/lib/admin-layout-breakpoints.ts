/** Viewport width at which the admin shell switches to mobile topbar + drawer (no persistent sidebar). */
export const ADMIN_MOBILE_NAV_MAX_WIDTH_PX = 1280;

export const ADMIN_MOBILE_NAV_MEDIA_QUERY = `(max-width: ${ADMIN_MOBILE_NAV_MAX_WIDTH_PX}px)`;
